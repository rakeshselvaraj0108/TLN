// Where the landing page's enquiry links point. Set NEXT_PUBLIC_CONTACT_EMAIL at build time to your own address;
// the fallback is a reserved .example address, deliberately not a real mailbox.
export const CONTACT_EMAIL = process.env.NEXT_PUBLIC_CONTACT_EMAIL || "contact@trace-x.example";
